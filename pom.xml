<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.sodex</groupId>
    <artifactId>root</artifactId>
    <version>1.0-SNAPSHOT</version>
    
    <!-- 若无，子module执行maven package时会提示must be "pom" but is "jar" -->
    <packaging>pom</packaging>

    <modules>
        <module>web-eureka</module>
        <module>web-gateway</module>
    </modules>

    <!-- spring boot -->
    <!-- Inherit defaults from Spring Boot -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.1.7.RELEASE</version>
    </parent>

    <!-- spring cloud 包管理-->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>Greenwich.RELEASE</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- spring boot -->
    <!-- Add typical dependencies for a web application -->
    <dependencies>
        <!-- 放在root pom中，会影响gateway模块的启动，因为gateway使用了webflux，而不是mvc -->
        <!--<dependency>-->
            <!--<groupId>org.springframework.boot</groupId>-->
            <!--<artifactId>spring-boot-starter-web</artifactId>-->
        <!--</dependency>-->
    </dependencies>

    <!-- spring boot -->
    <!-- 若有，maven package后的.jar可独立运行，.jar.original，与.jar比较，其中不包含依赖 -->
    <!-- 若无，maven package后的.jar不包含依赖 -->
    <!-- Package as an executable jar -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
